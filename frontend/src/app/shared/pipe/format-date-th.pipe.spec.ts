import { FormatDateThPipe } from './format-date-th.pipe';

describe('FormatDateThPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateThPipe();
    expect(pipe).toBeTruthy();
  });
});
